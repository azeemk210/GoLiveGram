# ============================================
# GOLIVEGRAM - FINAL APACHE / HOSTINGER CONFIG
# React SPA + SEO + Performance + Security
# ============================================

RewriteEngine On
RewriteBase /

# --------------------------------------------
# 1. FORCE WWW (MUST BE FIRST)
# --------------------------------------------
RewriteCond %{HTTP_HOST} ^golivegram\.com [NC]
RewriteRule ^(.*)$ https://www.golivegram.com/$1 [L,R=301]

# --------------------------------------------
# 2. SECURITY BASICS
# --------------------------------------------
Options -Indexes

<IfModule mod_headers.c>
  Header always set X-Frame-Options "DENY"
  Header always set X-Content-Type-Options "nosniff"
  Header always set X-XSS-Protection "1; mode=block"
  Header always set Referrer-Policy "strict-origin-when-cross-origin"
  Header unset Server
  Header unset X-Powered-By
</IfModule>

# --------------------------------------------
# 3. ENABLE GZIP COMPRESSION
# --------------------------------------------
<IfModule mod_deflate.c>
  AddOutputFilterByType DEFLATE text/plain
  AddOutputFilterByType DEFLATE text/html
  AddOutputFilterByType DEFLATE text/xml
  AddOutputFilterByType DEFLATE text/css
  AddOutputFilterByType DEFLATE application/javascript
  AddOutputFilterByType DEFLATE application/x-javascript
  AddOutputFilterByType DEFLATE application/json
  AddOutputFilterByType DEFLATE application/xml
  AddOutputFilterByType DEFLATE application/xhtml+xml
  AddOutputFilterByType DEFLATE image/svg+xml
  AddOutputFilterByType DEFLATE font/ttf
  AddOutputFilterByType DEFLATE font/otf
  AddOutputFilterByType DEFLATE font/woff
  AddOutputFilterByType DEFLATE font/woff2

  Header append Vary User-Agent
</IfModule>

# --------------------------------------------
# 4. BROWSER CACHING (EXPIRES)
# --------------------------------------------
<IfModule mod_expires.c>
  ExpiresActive On

  # HTML - no cache
  ExpiresByType text/html "access plus 0 seconds"

  # CSS & JS (hashed files)
  ExpiresByType text/css "access plus 1 year"
  ExpiresByType application/javascript "access plus 1 year"

  # Images
  ExpiresByType image/jpeg "access plus 1 year"
  ExpiresByType image/png "access plus 1 year"
  ExpiresByType image/webp "access plus 1 year"
  ExpiresByType image/svg+xml "access plus 1 year"
  ExpiresByType image/gif "access plus 1 year"
  ExpiresByType image/x-icon "access plus 1 year"

  # Fonts
  ExpiresByType font/ttf "access plus 1 year"
  ExpiresByType font/otf "access plus 1 year"
  ExpiresByType font/woff "access plus 1 year"
  ExpiresByType font/woff2 "access plus 1 year"

  # Media
  ExpiresByType video/mp4 "access plus 1 year"
</IfModule>

# --------------------------------------------
# 5. CACHE-CONTROL HEADERS
# --------------------------------------------
<IfModule mod_headers.c>
  <FilesMatch "\.(html|htm)$">
    Header set Cache-Control "no-cache, no-store, must-revalidate"
    Header set Pragma "no-cache"
    Header set Expires 0
  </FilesMatch>

  <FilesMatch "\.(css|js)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>

  <FilesMatch "\.(jpg|jpeg|png|gif|webp|svg|ico)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
  </FilesMatch>

  <FilesMatch "\.(ttf|otf|woff|woff2)$">
    Header set Cache-Control "public, max-age=31536000, immutable"
    Header set Access-Control-Allow-Origin "*"
  </FilesMatch>
</IfModule>

# --------------------------------------------
# 6. MIME TYPES
# --------------------------------------------
<IfModule mod_mime.c>
  AddType application/javascript js mjs
  AddType application/json json
  AddType image/webp webp
  AddType font/woff woff
  AddType font/woff2 woff2
  AddType font/ttf ttf
  AddType font/otf otf
</IfModule>

# --------------------------------------------
# 7. SEO-FRIENDLY SHARING ROUTES
# --------------------------------------------
RewriteRule ^recording/([a-zA-Z0-9\-]+)/?$ /index.html [L,QSA]
RewriteRule ^profile/([a-zA-Z0-9\-_\.]+)/?$ /index.html [L,QSA]
RewriteRule ^reel/([a-zA-Z0-9\-]+)/?$ /index.html [L,QSA]
RewriteRule ^live/([a-zA-Z0-9\-_\.]+)/?$ /index.html [L,QSA]

# --------------------------------------------
# 8. REACT SPA ROUTING (MUST BE LAST)
# --------------------------------------------
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteCond %{REQUEST_URI} !^/api/
RewriteCond %{REQUEST_URI} !^/admin/
RewriteRule ^ index.html [L]

# --------------------------------------------
# 9. UTF-8 CHARSET
# --------------------------------------------
AddDefaultCharset utf-8
